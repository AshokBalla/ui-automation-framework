name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CI runner
        run: |
          chmod +x .github/ci-runner.sh || true
          .github/ci-runner.sh
